- Finding 1: `get_run` tool metadata still says it reads runs "started with start_run", but the implemented runtime now also persists and exposes `run_subagent` one-shot snapshots by `run_id`. This is a semantic drift between the public tool contract and the actual lifecycle model.
- Finding 2: `scripts/run-observed-campaign.mjs` scopes failure logs, run outputs, run-task snapshots, input requests, sessions, and raw Pi sessions, but not `SUBAGENT007_MODEL_HEALTH_PATH`. The newly added one-shot health gate can therefore read or mutate default user health state during an observed campaign, contradicting the campaign isolation contract.
- Finding 3: Repo root contains `.DS_Store`, which is local Finder metadata and not part of the source, runtime, tests, or documentation contract.
- Finding 4: The June 11 SAF artifacts still read as pre-implementation: `reports/observed-real-use-trials-2026-06-11.md` has no historical/pre-implementation status note, `reports/full-coherent-revised-saf-set-2026-06-11.md` says the decision record does not implement runtime changes without noting that implementation later occurred, and `docs/plans/coherent-revised-saf-implementation-plan-2026-06-11.md` lacks implemented status. This can make completed repairs look pending or merely proposed.
- Finding 5: After source and script edits, the checked working runtime under `dist/` is stale until `npm run build` regenerates it. The registered MCP command and package tarball use `dist/server.js`, so source/runtime coherence requires a rebuild before final verification.

F001: Worktree completeness is local-only right now: implementation files are modified and four plan/report artifacts are untracked (`docs/plans/live-current-coherent-saf-implementation-plan-2026-06-11.md`, `reports/full-coherent-revised-saf-set-2026-06-11-live-current.md`, `reports/observed-real-use-horc-saf-campaign-2026-06-11-live-current.md`, `reports/saf-adversarial-stress-test-2026-06-11-live-current.md`). This is not a runtime defect, but any repository-durable completeness claim must include staging/commit or explicitly remain a local-worktree claim.
F002: Potential phase persistence cavity in `src/runTask.ts`: `getRunTask()` derives pending-input status from mailbox state and mutates in-memory `activePhase` to `input_required`, but the read path itself does not write a task snapshot. If pending input enters via mailbox state rather than a child `subagent007.input_request` event, a restart before another write may lose the derived `active_phase` even though `status` can still be recomputed.
F003: Confirmed lifecycle distortion in `src/runTask.ts`: `answerRunTaskInput()` records `input_answered` and returns a fresh view, but it does not move `active_phase` away from `input_required` when the answered request clears the pending mailbox. A run can therefore report `status:"working"` with `active_phase:"input_required"` until the child emits another line or terminates.
F004: Coverage-profile semantic waste in `scripts/observed-coverage-manifest.json`: `stateful-live` is a separately advertised live profile but contains only `tool-listing`, `model-listing`, and one-shot `success`; it is not stateful and is less complete than `live-current`, creating a misleading dead-profile label.
F005: Coverage-profile redundancy after F004 repair: `live-smoke` and `live-current` declare the same live-model scenarios and required surfaces. Keeping both as standalone profiles creates two names for one executable claim.
F006: Probe help contradiction after F005 repair: `scripts/run-observed-mcp-probe.mjs --help` still advertises `live-smoke` as a canonical profile name while also documenting it as a compatibility alias to `live-current`.
F007: Active plan status drift in `docs/plans/live-current-coherent-saf-implementation-plan-2026-06-11.md`: frontmatter still says `status: planned` and the ready criteria remain future-tense even though the implementation was applied and verified locally.
F008: Manifest validation cavity in `scripts/run-observed-mcp-probe.mjs`: `assertManifestComplete()` validates profiles and scenarios but does not validate that `aliases` point to existing profiles. After folding redundant live profile names into aliases, a typo in `aliases` could create a dead compatibility reference without failing `--help`.

- F009 [observed 2026-06-11] Full `npm test` fails in `tests/timeout-budget.test.ts` for `start_run returns timeout metadata and transcript before caller deadline`: expected one structured `recent_events` timeout event but observed zero. This indicates the run-scoped timeout public-event projection was bypassed after the lifecycle/phase repairs.
- F010 [observed 2026-06-11] Root refinement for F009: `getRunTask()` exposes terminal status as soon as `state.result` is assigned, before `appendTerminalEvent()` appends the structured terminal event and writes the final snapshot. Polling clients can see a terminal run with incomplete `recent_events`.
